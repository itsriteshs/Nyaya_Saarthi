# Project Datasets

This folder keeps the CSV data separate from the frontend source code.

- `datasets/` contains the current NyayaSaarthi CSV corpus and evaluation files.
- `src/data/` contains small TypeScript data files used by the frontend UI.

The frontend currently uses only mock/local TypeScript data. The CSV files are preserved for later backend, NLP and evaluation work.
